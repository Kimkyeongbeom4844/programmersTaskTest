# 사원정보테이블구축

## 💬 MEMO

```
 - addEventListener로 등록한 이벤트가 드롭다운을 통해 UI 가 업데이트 되면서 뒷정리로 removeEventListener를 사용하지 않아 메모리 누수 문제를 발견 -> 23.9.11 해결

 - DOM객체가 UI가 변경되고 난 후 기존 변경전 값을 기억하는지 자동적으로 업데이트 되는지 궁금함을 발견 -> 23.9.11 해결

 - try 문에서 return을 써도 finally에 내부에 있는 코드 실행됨
```
